import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

function Home() {
  return (
    <SafeAreaView>
      <Text>So maybe we call it a text component</Text>
    </SafeAreaView>
  );
}

export default Home;
