import { Loader } from "@mantine/core";
export const metadata = {
  title: "Purplenight",
  description: "Turn your favorite books into short blogs without losing exact lines",
};

export default function HaiJind() {
  return <Loader />;
}
